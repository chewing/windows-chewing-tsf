[workspace]
members = ["tip", "config", "tsfreg", "update", "xtask"]
exclude = ["editor", "preferences"]
resolver = "3"

[workspace.dependencies]
chewing = "0.11.0-alpha.1"
chewing_capi = "0.11.0-alpha.1"

[profile.release]
lto = "thin"
panic = "abort"
debug = true
