import { ConfigWindow } from "./config-window.slint";
import { EditorWindow, CallbackResult, ErrorKind } from "./editor-window.slint";

export { ConfigWindow, EditorWindow, CallbackResult, ErrorKind }
